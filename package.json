{
  "name": "lawlint",
  "private": true,
  "version": "0.1.0",
  "description": "A linter for AI-generated legal and general text.",
  "packageManager": "bun@1.3.14",
  "workspaces": ["apps/website"],
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "wasm:build": "bash scripts/build-wasm.sh",
    "build": "bun run --cwd apps/website build",
    "audit:security": "bun run scripts/audit-bun.mjs",
    "dev": "bun run --cwd apps/website dev",
    "check:commits": "bun run scripts/check-commit-messages.mjs",
    "lint": "bunx biome check apps/website",
    "typecheck": "bun run wasm:build && bunx tsc --noEmit -p apps/website/tsconfig.json",
    "test": "cargo test --workspace"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@conventional-commits/parser": "^0.4.1",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "fast-uri": "3.1.5",
    "ip-address": "10.5.0"
  }
}

{
  "name": "website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "bun run --cwd ../../packages/lawlint build && astro build"
  },
  "dependencies": {
    "astro": "^5.1.0",
    "lawlint": "workspace:*"
  }
}

---
// The downloads moved into the docs, alongside the install script and the
// build-from-source instructions they belong with. This keeps the old URL —
// it's linked from the README and from release notes — working.
import Redirect from "./_shared/Redirect.astro";
---

<Redirect to="/docs/installation" />

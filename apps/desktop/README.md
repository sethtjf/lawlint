# Archived desktop prototype

This Tauri application is retained as historical code for a possible future
desktop reintroduction. It is not part of the active Bun workspace, Rust CI,
release workflow, download page, or supported product surface.

The supported product today is the native `lawlint` CLI and the website/WASM
playground. Do not update this app as part of normal feature work. If desktop
support is revived, first restore its workspace/dependency policy and add
dedicated build, signing, and runtime tests before publishing artifacts.
